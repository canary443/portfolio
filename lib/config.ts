// feature flags (same as the prototype tweaks)

export const config = {
  showMap: true,      // hero dot-art image
  showAbout: true,    // about block
  spotlight: true,    // cursor glow on cards
  grain: true,        // film grain overlay
  adminLink: false    // admin link in the footer
};
